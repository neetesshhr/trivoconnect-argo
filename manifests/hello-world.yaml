# --- 1. The Namespace ---
apiVersion: v1
kind: Namespace
metadata:
  name: hello-world

---

# --- 2. The Deployment ---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-world
  namespace: hello-world
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hello
  template:
    metadata:
      labels:
        app: hello
    spec:
      containers:
      - name: hello-server
        image: nginxdemos/hello
        ports:
        - containerPort: 80

---

# --- 3. The Service ---
apiVersion: v1
kind: Service
metadata:
  name: hello-world-svc
  namespace: hello-world
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: hello

---

# --- 4. The Ingress (HTTP-only) ---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hello-world-ingress
  namespace: hello-world
  annotations:
    # --- THIS IS THE FIX ---
    # It must be a slash "/", not a dot "."
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: hello.trivoconnect.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: hello-world-svc
            port:
              number: 80